import { Color } from "./../../ui/colors";
import styled from "styled-components";

const SpentStringStyled = styled.h4``;

const SpentStyled = styled.h4`
  padding: 50px;
  background-color: ${Color.Primary};
`;

export { SpentStringStyled, SpentStyled };
